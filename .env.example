# Cloudflare Pages deploy (used by .github/workflows/deploy.yml)
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# Optional: GitHub token used by watchdog.yml to open an Issue on repeated scrape failures
# (GITHUB_TOKEN is provided automatically in Actions — only needed for local testing)
GITHUB_TOKEN=
